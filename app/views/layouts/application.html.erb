<!DOCTYPE html>
<html>
  <head>
    <title>Testblog</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="containers">
      <header class="clearfix">
        <ul>
          <li class="header_navi label label-default"><%= link_to "Home" ,"/" %></li>
          <li class="header_navi label label-primary"><%= link_to "ログを書く", new_blog_path %></li>
          <li class="header_navi label label-warning"><%= link_to "ログを見る", blogs_path %></li>
          <% if logged_in? %>
            <li class="header_navi label label-default"><%= link_to "ユーザー一覧", users_path %>
            <li class="header_navi label label-success"><%= link_to "プロフィール", user_path(current_user.id) %>
            <li class="header_navi label label-danger"><%= link_to "ログアウト", session_path(current_user.id),method: :delete %>
          <% else %>
            <li class="header_navi label label-success"><%= link_to "新規登録", new_user_path %>
            <li class="header_navi label label-danger"><%= link_to "ログイン", new_session_path %>
          <% end %>
        </ul>
      </header>
      <div class="title">
        <h1><%= link_to "わてぷログ" , "/" %></h1>
        <h3>学んだことを残しておくためのメモ</h3>
      </div>
  <main>
    <% flash.each do |key, value| %>
      <%= content_tag(:div, value, class:"#{key}") %>
    <% end %>
    <%= yield %>
  </main>
  <footer>
        <p>2018 わてぷログ</p>
  </footer>
  </body>
</html>
